describe('save()', function () {
  it('should error if product is missing', function () {

    (function () {
      require('..')()
    }).should.throwError('A string must be provided for \'product\'')

  })
  it('should fire a find event', function () {
    var save = require('..')('jim', { logger: { info: function () {} } })
    save.find({}, function () {})
  })
})
